import 'package:flutter/material.dart';

class MemberAttendaceCard extends StatelessWidget {
  const MemberAttendaceCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
