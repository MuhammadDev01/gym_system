import 'package:flutter/material.dart';

class OfftersView extends StatelessWidget {
  const OfftersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Offters View'));
  }
}
